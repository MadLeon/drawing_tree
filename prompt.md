未来实现
- customer 按照使用情况进行排序的功能
- 写脚本, 使用DIR Log中的数据批量更新图纸状态
- 在图纸界面, 用户能够点击创建按钮使用软件 UI 编写 MP (未来实现)

---

## 问题

### Order Entry Log 无法拾取所有输入的问题

- 研究代码, 高清生产数据库是否能自动将已不再活跃的数据自动标记
- 检查现有的 sync 脚本, 该脚本是否具有标记非活跃条目的功能

写可以复用的脚本, 用于手动更新数据库中 purchase_order.is_active 字段的值.

- 数据来自文件, 位置为 "\\rtdnas2\OE\Order Entry Log.xlsm"
- 使用开发数据库, 但应该很容易修改目标数据库的地址
- 该文件极度重要, 每次操作必须使用只读方式, 避免破坏该文件造成公司损失
- 该文件通常被其他用户打开, 即使用 UI 也只能选择只读方式
- 数据库中的 purchase_order.is_active 已经有值, 只需要对状态进行更新
- 该文件 AA 列记录了每个 order_item.id
- 提取该列信息并使用这些 id 更新数据库中 PO 的状态
  - 只有id对应的PO应该被认为是活跃的, 否则应标记为不活跃

### 视图切换问题

经过查看你生成的流程图, 我的想法

**前提**: 简洁视图和OE视图应该使用同一套数据, 在点击切换时, 只改变数据的呈现方式

#### 首次加载

##### 触发条件

- 情况1: 首次点击 Order Entry 按钮; 
- 情况2: 用户已在此界面, 点击刷新按钮 (后续实现)

两种情况均触发首次加载

##### 加载逻辑

- 加载该界面所有上层节点信息
  - 一个大的级联查询, 输出所有活跃的 order_item 的信息
  - 此时不包括可以被扩展的子节点, 逻辑应该与 PrefetchChildrenAsync() 类似, 将查找子节点信息放入异步中
  - MP / DIR 标记查询同样应该放到异步中 (你指出的优化方向)
  - GetPartIdsWithPdf() 不应该在网络硬盘中找内容, 应该查找的是数据库的 drawing_file 表 (请确认具体情况)

- 加载结束后, 应该cache所有查找到的信息

#### 视图切换

- 切换操作应该只涉及界面的重构, 不涉及数据操作, 是一个轻量功能

#### 更新

- 若使用 Edit 功能对 order_item 进行了更新, 应在更新数据库成功后仅更新缓存的内容, 并触发一次界面重新渲染
  - 这不应涉及数据的重新读取


你认为我的方案是否合适, 有什么建议


在非 All PO 页面做的修改在返回 PO 页面后无法正常更新

两个选择

1. 实现一个钩子机制, 在例如树构建之后触发, 渲染特定内容
2. 在 All PO 添加一个刷新按钮手动全量刷新