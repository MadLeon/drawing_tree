<Window x:Class="DrawingTree.Dialogs.AddDrawingDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Drawing"
        Width="420" Height="230"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Drawing Number -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="110"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Drawing Number *" VerticalAlignment="Center" FontSize="12"/>
            <TextBox  Grid.Column="1" x:Name="DrawingNumberBox" FontSize="12" Height="26" Padding="4,3"
                      TextChanged="DrawingNumberBox_TextChanged"/>
        </Grid>

        <!-- Revision -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="110"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Revision" VerticalAlignment="Center" FontSize="12"/>
            <TextBox  Grid.Column="1" x:Name="RevisionBox" FontSize="12" Height="26" Padding="4,3"/>
        </Grid>

        <!-- File Path -->
        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="110"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="6"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="File Path *" VerticalAlignment="Center" FontSize="12"/>
            <TextBox  Grid.Column="1" x:Name="FilePathBox" FontSize="12" Height="26" Padding="4,3"
                      TextChanged="FilePathBox_TextChanged"/>
            <Button   Grid.Column="3" Content="Browse" Width="60" Height="26" FontSize="12"
                      Click="BrowseButton_Click"/>
        </Grid>

        <!-- Buttons -->
        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="ConfirmButton" Content="Confirm"
                    Width="80" Height="28" IsEnabled="False"
                    Click="ConfirmButton_Click" Margin="0,0,8,0"/>
            <Button Content="Cancel" Width="80" Height="28"
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
