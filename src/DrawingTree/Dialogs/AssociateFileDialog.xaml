<Window x:Class="DrawingTree.Dialogs.AssociateFileDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Associate File" Width="460" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="MessageText" Grid.Row="0"
                   TextWrapping="Wrap" FontSize="12" Margin="0,0,0,14"/>

        <CheckBox x:Name="SetActiveCheck" Grid.Row="1"
                  Content="Set the selected file as active"
                  FontSize="11" Margin="0,0,0,16"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Width="70" Margin="0,0,8,0" IsCancel="True"
                    Click="CancelButton_Click"/>
            <Button Content="Select" Width="70" IsDefault="True"
                    Click="SelectButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
