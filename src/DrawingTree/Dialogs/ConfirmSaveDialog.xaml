<Window x:Class="DrawingTree.Dialogs.ConfirmSaveDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Confirm Save"
        Width="520"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Summary counts -->
        <TextBlock Grid.Row="0" Text="The following changes will be saved to the database:"
                   FontSize="13"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal">
            <Border Background="#E8F5E9" CornerRadius="4" Padding="10,4" Margin="0,0,8,0">
                <StackPanel Orientation="Horizontal">
                    <TextBlock x:Name="AddedCount" FontSize="16" FontWeight="Bold" Foreground="#388E3C"/>
                    <TextBlock Text=" added" FontSize="13" Foreground="#388E3C" VerticalAlignment="Bottom" Margin="2,0,0,1"/>
                </StackPanel>
            </Border>
            <Border Background="#FFEBEE" CornerRadius="4" Padding="10,4" Margin="0,0,8,0">
                <StackPanel Orientation="Horizontal">
                    <TextBlock x:Name="DeletedCount" FontSize="16" FontWeight="Bold" Foreground="#C62828"/>
                    <TextBlock Text=" deleted" FontSize="13" Foreground="#C62828" VerticalAlignment="Bottom" Margin="2,0,0,1"/>
                </StackPanel>
            </Border>
            <Border Background="#FFF8E1" CornerRadius="4" Padding="10,4">
                <StackPanel Orientation="Horizontal">
                    <TextBlock x:Name="ModifiedCount" FontSize="16" FontWeight="Bold" Foreground="#F57F17"/>
                    <TextBlock Text=" modified" FontSize="13" Foreground="#F57F17" VerticalAlignment="Bottom" Margin="2,0,0,1"/>
                </StackPanel>
            </Border>
        </StackPanel>

        <!-- Change detail text area -->
        <TextBox x:Name="ChangeDetail"
                 Grid.Row="4"
                 IsReadOnly="True"
                 AcceptsReturn="True"
                 TextWrapping="NoWrap"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 MaxHeight="200"
                 FontFamily="Consolas"
                 FontSize="12"
                 BorderThickness="1"
                 BorderBrush="#DDD"
                 Padding="8,6"
                 Background="#FAFAFA"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Save" Width="80" Height="28"
                    Margin="0,0,8,0" Click="SaveButton_Click"/>
            <Button Content="Cancel" Width="80" Height="28"
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
