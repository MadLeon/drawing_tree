<Window x:Class="DrawingTree.Dialogs.CurrentStepDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Update Current Step" Width="480" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Step selection -->
        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Current Step" FontSize="11" Foreground="#555555" Margin="0,0,0,3"/>
            <ComboBox x:Name="StepComboBox"
                      DisplayMemberPath="DisplayText"
                      Height="28"/>
        </StackPanel>

        <TextBlock Grid.Row="1" TextWrapping="Wrap" FontSize="11" Foreground="#777777"
                   Margin="0,0,0,14"
                   Text="Earlier steps are marked as completed. Trackers of later steps are removed."/>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Width="70" Margin="0,0,8,0"
                    Click="CancelButton_Click"/>
            <Button Content="Update" Width="70" IsDefault="True"
                    Click="UpdateButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
