<Window x:Class="DrawingTree.Dialogs.StepAssignmentDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Assign Step" Width="360" Height="320"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Step -->
        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Step" FontSize="11" Foreground="#555555" Margin="0,0,0,3"/>
            <ComboBox x:Name="StepComboBox"
                      DisplayMemberPath="DisplayText"
                      SelectedValuePath="Id"
                      Height="28"/>
        </StackPanel>

        <!-- Start date -->
        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="Start Date (required)" FontSize="11" Foreground="#555555" Margin="0,0,0,3"/>
            <DatePicker x:Name="StartDatePicker" Height="28"/>
        </StackPanel>

        <!-- End date -->
        <StackPanel Grid.Row="2" Margin="0,0,0,10">
            <TextBlock Text="End Date (optional)" FontSize="11" Foreground="#555555" Margin="0,0,0,3"/>
            <DatePicker x:Name="EndDatePicker" Height="28"/>
        </StackPanel>

        <!-- Mark previous complete -->
        <CheckBox x:Name="MarkPreviousCheck" Grid.Row="3"
                  Content="Mark all previous steps as complete"
                  FontSize="11" Margin="0,4,0,0" VerticalAlignment="Top"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Width="70" Margin="0,0,8,0"
                    Click="CancelButton_Click"/>
            <Button Content="OK" Width="70" IsDefault="True"
                    Click="OkButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
